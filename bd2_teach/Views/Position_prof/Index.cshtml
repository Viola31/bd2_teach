@model IEnumerable<bd2_teach.Models.Position_prof>

@{
    ViewBag.Title = "Index";
}

<h2>Position</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Pos_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Pos_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Position_prof_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Position_prof_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Position_prof_id })
        </td>
    </tr>
}

</table>
