@model IEnumerable<bd2_teach.Models.Discipline>

@{
    ViewBag.Title = "Index";
}

<h2>Discipline</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Dis_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Dis_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Discipline_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Discipline_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Discipline_id })
        </td>
    </tr>
}

</table>
